# Copy to .env.local for `npx expo start`. EXPO_PUBLIC_ values are built into
# the app bundle, so never put secrets here.
EXPO_PUBLIC_API_URL=https://api-production-eb96.up.railway.app
EXPO_PUBLIC_SHOW_DEV_SCREENS=true
