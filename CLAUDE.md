@AGENTS.md

Project skills live in `.claude/skills/` (also reachable as `.agents/skills/` for Codex and Cursor):

- `software-factory` runs the gated delivery workflow described in AGENTS.md.
- `mobile-plan-mvp` scopes a mobile release to its smallest complete journey.
