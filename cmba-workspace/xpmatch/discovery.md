# Discovery — XPMatch: skipped

Skill: `cmba-discovery` (stage 8 of `cmba-run`). **Not run.**

**Why it was skipped:**
- Gate 0 (`idea.md`, Step 0) classified XPMatch as PARTIAL and named `cmba-discovery` as a stage that must not run.
- No service is in scope. XPMatch is a product, the beta is free, and the traveler never talks to the operator (PRD §1, plan §4).

**Revisit it** only if the founder runs the PRD's separate concierge experiment (`offer.md`, the first paid rung). A hand-built plan is a service, and the discovery-call method would then apply to it.

**Downstream effect:**
- `email.md` must not assume any contact detail that a discovery form would have collected.
- The only capture forms are the beta waitlist (`profile.md`) and the magnet page (`lead-magnet.md`).
